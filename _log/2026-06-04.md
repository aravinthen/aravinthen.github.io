---
layout: log 
title: (sting) Ray
date: 2026-06-04
math: True
tags: [reinforcement-learning, systems]
---

## Summary
1. Went through the Ray Core course.

## Notes
### Systems
1. All makes sense so far. It's pretty magic how Ray just up and goes - I've never really given much thought into how a Ray cluster is *actually built*. I mean... just chucking up `ray.init()` is fine, but how do you actually configure a `ray` to `init()`?
2. Decorators in this context are magic. Really, really nice example. It's not too different in spirit from `multiprocessing.Pool`, although `Pool` uses a map. 
3. And a Ray `Actor` is merely a class version of the function. This looks nice so far. :) 

## Remarks
1. Quiet day today - had to work through my lunchbreak. Knocked out after work too, so I didn't manage to get any reinforcement learning done. 
2. That's okay. We're getting there, bit by bit. 